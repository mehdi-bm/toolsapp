/// Keep in sync with the `version:` field in pubspec.yaml.
const String kAppVersion = '1.0.0';

const String kContactEmail = 'devteam.parsikhesab@gmail.com';

/// Keep in sync with android:label in AndroidManifest.xml and the
/// `description:` field in pubspec.yaml, which can't reference this
/// constant directly.
const String kAppName = 'جعبه‌ابزار پارسیک';

const String kAppTagline = 'ابزارهای کاربردی، همیشه در دسترس شما';

const String kAppDescription =
    '$kAppName مجموعه‌ای از ابزارهای کاربردی روزمره را در یک برنامه ساده و '
    'مدرن در اختیار شما قرار می‌دهد.';
